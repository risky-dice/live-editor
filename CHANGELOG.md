# 변경 이력

버전 표기는 스킬 패키지 기준입니다. 날짜는 패키징 시점.

## v1.6.1

이름을 하나로 합쳤습니다. `hanvas`라는 이름을 두 물건이 나눠 쓰고 있었습니다 — `hwpedit.mjs`의
구버전 토글 뷰어와, 실제로 보여주는 자립형 스튜디오를 굽는 `hanvas_full.py`입니다. 구버전은 이미
"보여주는 미리보기가 아니다"라고 격하돼 있었고 값싼 중간 렌더는 `render`·`svg`가 하므로, 47줄짜리
구버전 명령을 지우고 `hanvas_full.py`를 `hanvas.py`로 옮겼습니다. 동작 변화는 없고 셀프테스트는
26개에서 25개로 줄었습니다(지운 항목 하나만큼).

## v1.6

미리보기 파일을 **실제 사용자 컴퓨터에서** 써 본 결과를 반영했습니다. v1.5까지는 "다운로드해서
크롬으로 열면 버튼이 살아난다"고 적어놨지만, 실제로 그 경로를 밟아보면 세 군데에서 막혔습니다.

**빈 창이 뜨는 문제 — 원인은 다운로드였습니다.** rhwp 확장이 `.hwpx` 다운로드를 가로채 뷰어로 열려고
하는데, 저장에 쓰던 `blob:` 주소가 확장이 거부하는 scheme이라 "파일 로드 실패: 허용되지 않은 URL
scheme" 창이 매번 하나씩 더 떴습니다. 저장을 File System Access API(`showSaveFilePicker` 또는
디렉터리 핸들 직접 쓰기)로 바꿔 다운로드 기록 자체를 만들지 않게 했습니다. 가로챌 대상이 없으면
가로채이지 않습니다. `<a download>` 방식은 미지원 브라우저용 최후 수단으로만 남겼습니다.

첫 진단은 틀렸었습니다 — 제스처 유지용 `about:blank` 탭이 범인인 줄 알고 rhwp 연동을 2단계 클릭으로
바꿨는데도 빈 창이 떴습니다. 2단계 구조는 그 자체로 옳아서(빈 탭을 아예 만들지 않음) 남겼지만,
실제 원인은 그 아래 있던 blob 다운로드였습니다.

**윈도우 경로.** `'file://' + 'C:/…'`는 `C:`를 호스트로 파싱해 확장이 거부합니다. 드라이브 문자가
있으면 `file:///`로 정규화합니다.

**크롬은 다운로드 폴더를 막습니다.** File System Access API로 다운로드·바탕화면·문서 폴더를 고르면
"이 폴더에는 시스템 파일이 있어서 사용할 수 없습니다"가 뜹니다. 하위 폴더는 허용됩니다. 그래서
작업 폴더는 반드시 `Downloads/<주제>` 같은 하위 폴더로 잡도록 SKILL.md에 못박고, 📁 **작업 폴더 지정**
버튼을 새로 뒀습니다. 한 번 지정하면 이후 저장은 대화상자 없이 그 폴더에 바로 씁니다.

**🗑 수정본 정리 추가.** 편집을 반복하면 `문서_143022.hwpx`가 폴더에 쌓입니다. 현재 문서 이름으로
시작하는 hwpx를 모아 수정 시각순으로 정렬하고, 가장 최근 것만 체크 해제한 목록을 보여준 뒤 확인을
받아 삭제합니다. 파일명·시각·크기를 눈으로 확인하고 누르는 구조라 최종본이 실수로 날아가지 않습니다.
휴지통을 거치지 않는 영구 삭제라는 점은 사용자에게 반드시 알립니다.

**윈도우에서 익스플로러로 열리는 문제.** `preview.html`을 더블클릭하면 기본 연결 프로그램이 열립니다.
크롬을 찾아 실행하는 배치 파일 조각을 SKILL.md에 실어, 미리보기와 같은 폴더에 함께 배치하도록 했습니다.

## v1.5

미리보기를 **하나로 합쳤습니다.** 실사용에서 나온 지적이 정확했습니다.

**"버튼이 안 보인다."** 그때까지 미리보기 화면이 세 종류였습니다 — 빠른 블록 미리보기(`render`),
토글 뷰어(`hanvas`), 자립형 편집기(`hanvas_full.py`). 그런데 문서 크기로 자동 선택하는 규칙이 있어서
129블록짜리 실문서는 버튼이 아예 없는 `render`로 갔습니다. 스킬이 만들어 둔 편집 버튼을 사용자가
볼 수 없는 게 당연했고, 이건 한 번의 실수가 아니라 설계 결함이었습니다.

**"두 개나 뜨는 이유가 뭐야?"** 부족한 화면을 메우려고 파일을 하나 더 보내면 대화창에 카드가 둘
뜹니다. 어느 쪽이 진짜 살아 있는 화면인지 사용자가 알 방법이 없습니다.

그래서 크기 규칙을 손보는 대신 화면 자체를 하나로 만들었습니다. 이제 문서 크기·환경과 무관하게
`hanvas_full.py`가 만든 파일 **하나만** 보여줍니다. 두 번째 미리보기 파일을 곁들여 보내지 않습니다.
`hanvas`·`svg`·`render`는 명령으로는 남지만, 사용자에게 보이는 미리보기는 아닙니다.

**엔진 없이도 다 보이게.** 합치려면 먼저 고칠 게 있었습니다. 아티팩트 창은 WebAssembly를 막는데,
그 환경에서 자립형 편집기는 "보기 전용으로 내려간다"는 설명과 달리 빨간 오류 상자를 띄웠고 깔끔 뷰는
아예 죽었습니다. 이제 원본 뷰(SVG)와 깔끔 뷰(HTML)를 **빌드 시점에 미리 렌더해** 파일 안에 gzip으로
심습니다. 형광펜도 이때 칠해 두므로 편집 후 바뀐 자리가 보기 전용 모드에서도 그대로 보입니다.
엔진이 없으면 눌러도 아무 일 없는 저장·연동 버튼은 감추고 뷰 전환만 남깁니다 — 죽은 버튼을 남겨두면
파일이 고장난 줄 압니다.

**`prerender` 명령 추가.** 위 재료(SVG 배열 + 깔끔 뷰 HTML + 형광펜)를 한 번에 뽑습니다. 하이라이트
로직이 한 군데에만 있게 되어 뷰마다 표시가 어긋날 여지가 없어졌습니다.

**셀프테스트 26종.** "엔진 없이도 두 뷰가 다 있는가", "prerender가 형광펜까지 칠해 내보내는가"
두 항목이 추가됐습니다. 17쪽 실문서로 WASM이 있는 환경과 막은 환경 양쪽에서 헤드리스 검증했습니다.

## v1.4

실문서 리허설에서 나온 두 가지를 고쳤습니다. 합성 샘플로는 절대 안 나오던 것들입니다.

**형광펜이 조용히 사라지던 문제.** 학교·관공서 양식처럼 장평(자간 축소)이 걸린 문서에서는 글자가
`x`/`y` 대신 `transform="translate(...) scale(...)"`으로 찍힙니다. 실측한 공문서 한 쪽에서는 글자
397개 중 279개가 이 경로였는데, 하이라이트 코드가 `x`/`y`만 보고 있어서 **편집은 됐는데 형광 표시만
안 깔리는** 상태가 됐습니다. 사용자 입장에선 "안 바뀐 것"으로 보입니다. translate/scale/matrix를 모두
읽도록 고쳤고, 장평 배율을 반영해 표시 폭도 맞췄습니다. 덤으로 SVG가 공백을 글자로 안 찍는 문제도
잡아서 "조정될 수 있음"처럼 띄어쓰기가 든 치환문에도 형광펜이 붙습니다. 그래도 못 찾은 부분이 있으면
이제 `warnings`로 알립니다 — 조용히 넘어가지 않습니다.

**표 셀 줄바꿈 겹침 경고.** 표 셀 안에 명시적 줄바꿈이 있으면 렌더 엔진이 여러 줄을 같은 baseline에
겹쳐 찍습니다("(단원명)/(영역명)" → "(단영역원명명)"). rhwp 0.7.19와 0.8.0 모두 같아서 이쪽에서 고칠
수 있는 문제가 아닙니다. 대신 겹친 글자를 세어 감지하고 "원본 뷰 렌더 한계이며 문서와 편집 결과에는
이상이 없다, 그 부분은 깔끔 뷰로 보라"고 경고를 붙입니다. 17쪽 실문서에서 10쪽·315자를 잡아냈고,
표 47개짜리 다른 실문서와 샘플 문서에서는 오탐이 없었습니다.

**셀프테스트 24종.** 위 두 가지가 회귀하지 않도록 "띄어쓰기 든 문자열 형광 표시"와 "정상 문서에
겹침 경고 오탐 없음" 항목이 추가됐습니다.

## v1.3

트리거 정확도, 용량, 배포 문서를 손봤습니다.

**트리거 튜닝.** 30개 질의(트리거 16 / 비트리거 14)로 실제 스킬 선택을 측정했더니
"증명서.pdf 금액 수정해서 파일로 줘" 같은 PDF 편집 요청이 일반 `pdf` 스킬로 새는 경로가
하나 있었습니다. description에 "PDF 규칙"을 넣어 수정/변경/교체/바꿔/고쳐/replace는 이 스킬,
병합·분할·워터마크·양식·OCR·새 PDF 생성은 `pdf` 스킬로 명시했습니다. 겹치는 다른 스킬이
없는 환경에서 30개 중 29개(트리거 15/16, 비트리거 14/14)가 의도대로 동작합니다.

**용량 1/4.** 자립형 편집기(`hanvas_full.py`)가 만들던 HTML이 19쪽 실문서 기준 16.5MB에서
4.2MB로 줄었습니다. 7MB짜리 rhwp WASM과 수 MB의 사전 렌더 SVG를 각각 gzip으로 묶어
base64로 심고, 브라우저에서 `DecompressionStream('gzip')`으로 풀도록 바꿨습니다.
이 API가 없는 브라우저에서는 빈 화면 대신 "크롬/엣지 최신 버전이나 사파리 16.4 이상에서
열어주세요"라는 안내가 뜹니다. WASM 압축 해제만 실패하면 보기 전용으로 계속 동작합니다.

**셀프테스트 22종.** `hanvas_full.py`까지 실제로 빌드해 보고, gzip 상수 두 개가 살아 있는지와
결과 HTML이 8MB 미만인지 확인하는 항목이 추가됐습니다. 용량 회귀가 조용히 지나가지 않습니다.

**문서.** `README.md`(설치·확인·한계·구성 파일)와 이 `CHANGELOG.md`를 추가했습니다.

## v1.2

견고성 강화와 셀프테스트 도입.

실패를 조용히 삼키지 않도록 전 경로를 점검했습니다. 암호 걸린 문서는 즉시 거절하고 암호를
묻지 않으며, 300쪽 초과 문서는 `HWPEDIT_PAGE_CAP`까지만 렌더하고 잘린 사실을 `warnings`에
남깁니다. 의존성 버전이 검증본과 다르면 실패 대신 경고를 붙여 진행합니다. 찾는 문자열이
없거나 여러 곳에서 중복될 때, 표 셀 경계를 넘는 편집을 시도할 때의 오류 메시지를 모두
사람이 읽을 수 있는 한국어로 통일했습니다.

`node hwpedit.mjs test`가 생겼습니다. 샘플 문서를 즉석에서 만들어
`blocks → render → edit → apply → svg → hanvas` 전 과정과 오류 경로 9종을 돌리고 21개 항목의
통과/실패 표를 찍습니다. 낯선 환경에서 "환경 문제인지 문서 문제인지"를 30초 안에 가릅니다.

## v1.1

`hwpx-live-editor`와 `pdf-live-editor`를 하나의 `live-editor`로 통합했습니다.
두 스킬이 서로를 가리는 문제가 있었고, 사용자 입장에서는 "문서 고쳐줘"라는 한 가지 일이었습니다.

Hanvas 뷰 토글이 들어왔습니다. 한컴 레이아웃 그대로의 원본 뷰와, 읽기 좋게 정돈한 깔끔 뷰를
오가며 볼 수 있습니다. 브라우저 혼자 도는 자립형 편집기 HTML(`hanvas_full.py`)도 이때 추가됐습니다.

## v1.0

`hwpx-live-editor`(HWP/HWPX)와 `pdf-live-editor`(텍스트 PDF)로 시작했습니다.
문단·표 셀 단위 찾아바꾸기, 편집 위치에 형광펜이 칠해진 SVG 미리보기, 편집본 파일 반환이라는
기본 루프가 이때 완성됐습니다.
